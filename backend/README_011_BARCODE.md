# 011 — QR o código de barras

Ejecuta `011-barcode-format.sql` una sola vez.

Agrega `barcode_format` a cada tarjeta (`qr` o `code128`) y actualiza el RPC público para que la tarjeta web use el formato seleccionado.
